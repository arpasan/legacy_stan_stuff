# AGENTS.md — legacy Stan examples

- `projects/<slug>/` — source Rmd, data, Stan helpers
- `docs/` — GitHub Pages HTML
- Prefer `rstanarm` / CmdStanR diagnostics; show all code in renders
- Large raw downloads (e.g. full Capital One `transactions.txt`) stay gitignored; use samples + README fetch notes
